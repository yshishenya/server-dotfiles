# 🚀 Быстрый старт - Установка production сервера

> Автоматическая настройка Ubuntu сервера за 20-30 минут с полным набором современных инструментов

## 📋 Что вы получите

После установки у вас будет:
- ✅ **Docker** + Docker Compose
- ✅ **Nginx** + Certbot (SSL) + Fail2ban
- ✅ **Zsh** + Oh My Zsh + Powerlevel10k + плагины
- ✅ **Python** 3.11.9, 3.12.3 (через pyenv)
- ✅ **Node.js** 18, 20 (через nvm)
- ✅ **Современные утилиты**: bat, eza, fzf, ripgrep, fd, lazygit, lazydocker, delta, zoxide, btop, tldr, tmux
- ✅ **44 умных подсказки** для изучения новых возможностей
- ✅ **15+ полезных функций** (mkcd, extract, backup, vif, cdf, gdiff, dexec...)
- ✅ **Структура проектов** /opt/projects и /opt/backups
- ✅ **Утилиты управления** сервером

---

## 🎯 Установка (3 шага)

### Шаг 1: Подключитесь к серверу

```bash
ssh your-user@your-server
```

### Шаг 2: Склонируйте репозиторий

```bash
cd ~
git clone https://github.com/yshishenya/server-dotfiles.git
cd server-dotfiles
```

### Шаг 3: Запустите установку

```bash
chmod +x setup-production-server.sh
sudo ./setup-production-server.sh
```

**⏱️ Время выполнения:** 20-30 минут

**💡 Что делает скрипт:**
1. Обновляет систему
2. Устанавливает все пакеты и утилиты
3. Настраивает Docker, Nginx, Certbot, Fail2ban
4. Устанавливает Zsh + Oh My Zsh + Powerlevel10k
5. Применяет dotfiles (`.zshrc`, `.p10k.zsh`)
6. Устанавливает pyenv + Python версии
7. Устанавливает nvm + Node.js версии
8. Создает структуру проектов
9. Настраивает **вашего текущего пользователя** со всеми фичами
10. Создает утилиты управления сервером

---

## 🎉 После установки

### 1. Активируйте Zsh

```bash
exec zsh
```

Вы увидите:
- 🎨 Красивый Powerlevel10k prompt
- 💡 Возможно случайную подсказку о новых возможностях

### 2. Настройте Git

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### 3. Попробуйте новые команды!

```bash
# 💡 Посмотреть подсказки
tips              # случайная подсказка
tips-all          # все 44 подсказки

# 📂 Умная навигация
projects          # перейти без cd (AUTO_CD)
..                # cd ..
...               # cd ../..
z projects        # умный прыжок (zoxide)
d                 # показать stack директорий

# 📄 Красивые файлы
ls                # eza с иконками и цветами
ll                # длинный список + Git статус
cat file.js       # bat с подсветкой синтаксиса
grep "text"       # ripgrep (быстрее в 10x)

# 🔍 FZF магия
Ctrl+R            # поиск в истории
Ctrl+T            # выбрать файл
Alt+C             # выбрать директорию
vif               # vim + fzf выбор файла

# 🐳 Docker
ld                # lazydocker (TUI)
dps               # красивая таблица контейнеров
dcu               # docker compose up -d
dcd               # docker compose down
dexec             # войти в контейнер через fzf

# 📊 Git
lg                # lazygit (TUI)
gdiff             # интерактивный просмотр коммитов
gd                # git diff (с delta)

# 🛠️ Полезные функции
mkcd test/path    # создать папки и перейти
extract file.zip  # распаковать любой архив
backup file.conf  # бэкап с timestamp
dirsize           # размер директорий
myip              # ваш внешний IP
```

### 4. Проверьте установку

```bash
# Python
pyenv versions

# Node.js
nvm list

# Docker
docker --version
docker compose version

# Утилиты
eza --version
bat --version
lazygit --version
btop
```

---

## 📚 Изучение возможностей

### Система подсказок

Автоматические подсказки появляются каждый 5-й запуск shell:

```bash
💡 Нажми Ctrl+R для поиска в истории команд через fzf
💡 'mkcd path/to/dir' создаст все папки и перейдет в них
💡 'lg' запускает lazygit - визуальный TUI для Git
```

Вызов вручную:
```bash
tips              # случайная подсказка
tips-all          # все подсказки сразу
```

### Полная документация

```bash
# Все крутые фичи с примерами (350+ строк)
bat ~/server-dotfiles/ZSH-FEATURES.md

# Или откройте в браузере на GitHub
```

**📖 Документация включает:**
- Умная навигация (AUTO_CD, pushd, zoxide)
- Улучшенная история (50k строк, без дубликатов)
- 15+ полезных функций
- FZF интеграция
- Docker & Git workflow
- Расширенный globbing
- Трюки и советы

---

## 🔧 Настройка проектов

### Создание проекта

```bash
cd /opt/projects
sudo mkdir my-project
sudo chown $USER:dev_team my-project
cd my-project
git clone https://github.com/user/repo.git .
```

### Настройка Nginx

```bash
# 1. Создайте конфиг
sudo nano /etc/nginx/sites-available/myproject.com

# 2. Включите сайт
sudo ln -s /etc/nginx/sites-available/myproject.com /etc/nginx/sites-enabled/

# 3. Проверьте конфиг
sudo nginx -t

# 4. Перезагрузите
sudo systemctl reload nginx

# 5. Получите SSL
sudo certbot --nginx -d myproject.com
```

### Пример Nginx конфига

```nginx
server {
    listen 80;
    server_name myproject.com www.myproject.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

---

## 🛠️ Утилиты управления сервером

```bash
# Статус всех проектов и Docker контейнеров
project-status

# Бэкап всех Docker volumes
backup-docker-volumes

# Бэкап всех проектов
backup-projects
```

---

## 🏗️ Структура сервера

```
/opt/
├── projects/              # Ваши проекты
│   ├── project1/
│   ├── project2/
│   └── ...
└── backups/               # Автоматические бэкапы
    ├── docker-volumes/
    └── projects/

/home/
└── your-user/             # Ваш пользователь
    ├── .zshrc             # Конфигурация Zsh
    ├── .p10k.zsh          # Тема Powerlevel10k
    ├── .pyenv/            # Python версии
    ├── .nvm/              # Node.js версии
    └── dotfiles/          # Клон репозитория
```

---

## 🆘 Устранение проблем

### Nginx: duplicate directive

Если при установке была ошибка с Nginx:

```bash
# Удалить дубликаты
sudo sed -i '0,/client_max_body_size/b; /client_max_body_size/d' /etc/nginx/nginx.conf

# Проверить
sudo nginx -t

# Перезапустить
sudo systemctl restart nginx
```

### Git коммиты идут не от того пользователя

```bash
# Проверить текущие настройки
git config --global user.name
git config --global user.email

# Изменить
git config --global user.name "Your Name"
git config --global user.email "your@email.com"
```

### Docker требует sudo

```bash
# Добавить себя в группу docker
sudo usermod -aG docker $USER

# Перелогиниться или
newgrp docker
```

### Zsh плагины не работают

```bash
# Переустановить плагины
~/dotfiles/install-plugins.sh

# Перезагрузить shell
exec zsh
```

### Утилита не найдена

Проверьте что скрипт завершился успешно:

```bash
# Проверить установленные утилиты
which eza bat rg fd zoxide lazygit lazydocker

# Если что-то отсутствует - переустановить вручную
sudo apt update
sudo apt install -y bat fzf ripgrep fd-find
```

---

## 🔄 Обновление dotfiles

Если в репозитории появились обновления:

```bash
cd ~/server-dotfiles
git pull

# Применить новый .zshrc
cp .zshrc ~/.zshrc

# Применить новую тему
cp .p10k.zsh ~/.p10k.zsh

# Перезагрузить shell
exec zsh
```

---

## 📖 Дополнительная документация

В репозитории:
- **README.md** - обзор всех возможностей
- **ZSH-FEATURES.md** - полное руководство (350+ строк примеров)
- **CHECKLIST.md** - детальный чеклист настройки
- **setup-production-server.sh** - сам скрипт (комментарии внутри)

---

## ⚠️ Важные замечания

1. **SSH безопасность**
   - После установки SSH работает только с ключами
   - Пароли отключены
   - Root вход отключен

2. **Перезапуск SSH**
   - Не закрывайте текущую сессию до проверки нового подключения
   - Откройте новое окно терминала и проверьте вход
   - Только потом делайте `sudo systemctl restart sshd`

3. **Группа docker**
   - Для работы без sudo нужно перелогиниться
   - Или выполнить `newgrp docker`

4. **История команд**
   - 50,000 строк без дубликатов
   - Команды с пробелом в начале НЕ сохраняются (для секретов)
   - Общая между всеми сессиями

5. **Безопасность команд**
   - `rm`, `cp`, `mv` теперь спрашивают подтверждение
   - Для быстрого выполнения используйте `\rm`, `\cp`, `\mv`

---

## 🎓 Что дальше?

1. ✅ Прочитайте **ZSH-FEATURES.md** - узнаете о всех возможностях
2. ✅ Пробуйте команды из подсказок (`tips`)
3. ✅ Используйте `Ctrl+R` для поиска в истории
4. ✅ Настройте свои проекты
5. ✅ Наслаждайтесь продуктивностью! 🚀

---

## 💬 Поддержка

При возникновении проблем:

1. Проверьте логи: `journalctl -xe`
2. Проверьте статус сервисов: `systemctl status docker nginx fail2ban`
3. Посмотрите issues в репозитории: https://github.com/yshishenya/server-dotfiles/issues
4. Создайте новый issue с описанием проблемы

---

**Версия:** 2.0
**Обновлено:** 2025-01-03
**Автор:** Yan Shishenya
**Репозиторий:** https://github.com/yshishenya/server-dotfiles

**Приятной работы!** 🎉
