# Server Dotfiles

Общие настройки для production серверов команды разработки + автоматический скрипт полной настройки сервера.

## Содержимое

### Конфигурационные файлы
- `.zshrc` - основная конфигурация Zsh
- `.p10k.zsh` - настройки темы Powerlevel10k (91KB)

### Скрипты
- `setup-production-server.sh` - **ПОЛНАЯ автоматическая настройка нового сервера** (31KB, 773 строки)
- `install-plugins.sh` - установка плагинов Oh My Zsh
- `install.sh` - быстрая установка dotfiles на существующий сервер

### Документация
- `QUICK-START.md` - быстрый старт для нового сервера
- `CHECKLIST.md` - полный чеклист настройки
- `ZSH-FEATURES.md` - **полное руководство по всем крутым фичам Zsh** (350+ строк с примерами)
- `cleanup-users.sh` - скрипт удаления старых пользователей

## Что включено

### Тема
- **Powerlevel10k** - современная и быстрая тема

### Плагины
- **git** - Git интеграция и алиасы
- **zsh-autosuggestions** - автодополнение на основе истории
- **zsh-syntax-highlighting** - подсветка синтаксиса команд
- **supervisor** - интеграция с Supervisor
- **sudo** - быстрое добавление sudo (ESC ESC)
- **z** - быстрая навигация по директориям

### Алиасы
```bash
ls='exa'           # современная замена ls
ll='exa -l'        # длинный формат
la='exa -la'       # все файлы
ltree='exa -T'     # древовидный вид
fd='fdfind'        # быстрый поиск
c="clear"          # очистка
x="exit"           # выход
n="nano"           # редактор
```

### Интеграции
- **pyenv** - менеджер версий Python
- **nvm** - менеджер версий Node.js
- **bun** - быстрый JavaScript runtime

### 💡 Система подсказок
- **44 полезных подсказки** о новых командах и фичах
- Автоматически показывает случайную подсказку каждые 5 запусков shell
- Команды: `tips` (случайная), `tips-all` (все сразу)
- Помогает постепенно изучать новые возможности!

### 🎯 Крутые возможности Zsh

**Умная навигация:**
- `AUTO_CD` - переход без cd (просто введите имя папки)
- `..` / `...` / `....` - быстрый подъем вверх
- `z projects` - прыжок в часто используемые директории (zoxide)
- Stack директорий - `d` показать, `cd -1` вернуться

**Улучшенная история:**
- 50,000 команд без дубликатов
- Общая между всеми сессиями
- Команды с пробелом не сохраняются (для секретов)
- `Ctrl+R` - поиск через fzf с preview

**Полезные функции (15+):**
- `mkcd path` - создать папки и перейти
- `extract file.zip` - распаковать любой архив
- `backup file` - быстрый бэкап с timestamp
- `vif` - vim + fzf выбор файла
- `cdf` - cd + fzf выбор директории
- `gdiff` - git log + diff через fzf
- `dexec` - войти в Docker контейнер (fzf)
- и многое другое...

**FZF интеграция:**
- `Ctrl+R` - поиск в истории
- `Ctrl+T` - выбор файла
- `Alt+C` - выбор директории
- Красивая Dracula тема

📖 **Полная документация:** [ZSH-FEATURES.md](ZSH-FEATURES.md)

## 🚀 Полная автоматическая установка нового сервера

**Рекомендуемый способ для нового сервера!**

Скрипт `setup-production-server.sh` полностью настраивает production сервер за 20-30 минут:

### Что устанавливается автоматически:
- ✅ Пользователи разработчиков (yan, alex, deploy) с SSH ключами
- ✅ Docker + Docker Compose
- ✅ Python (pyenv + версии 3.11.9, 3.12.3)
- ✅ Node.js (nvm + версии 18, 20)
- ✅ Nginx + Certbot (SSL)
- ✅ Fail2ban (защита SSH)
- ✅ Zsh + Oh My Zsh + Powerlevel10k + плагины
- ✅ Современные утилиты: bat, eza, fzf, ripgrep, fd, lazygit, lazydocker, delta, zoxide, btop, tldr, tmux
- ✅ Структура проектов в /opt/projects и /opt/backups
- ✅ Утилиты управления: project-status, backup-docker-volumes, backup-projects

### Использование:

```bash
# На новом сервере
ssh root@NEW_SERVER_IP
cd /root
git clone https://github.com/yshishenya/server-dotfiles.git
cd server-dotfiles
chmod +x setup-production-server.sh
sudo ./setup-production-server.sh
```

**Время установки:** 20-30 минут

📖 Подробные инструкции в файлах [QUICK-START.md](QUICK-START.md) и [CHECKLIST.md](CHECKLIST.md)

## Установка только dotfiles (на существующий сервер)

### Автоматическая (через главный скрипт)
```bash
curl -fsSL https://raw.githubusercontent.com/yshishenya/server-dotfiles/main/install.sh | bash
```

### Ручная
```bash
# Клонировать репозиторий
git clone https://github.com/yshishenya/server-dotfiles.git ~/dotfiles

# Установить Oh My Zsh (если еще не установлен)
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

# Установить плагины
~/dotfiles/install-plugins.sh

# Скопировать конфиги
cp ~/dotfiles/.zshrc ~/.zshrc
cp ~/dotfiles/.p10k.zsh ~/.p10k.zsh

# Применить изменения
source ~/.zshrc
```

## Индивидуальные настройки

### Git конфигурация
Git конфигурация (.gitconfig) **НЕ** хранится в этом репозитории - каждый пользователь настраивает свою:

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### Дополнительные алиасы
Если хотите добавить свои алиасы, создайте файл `~/.zshrc.local`:

```bash
# ~/.zshrc.local
alias myalias='my command'
```

И добавьте в конец `~/.zshrc`:
```bash
# Load local config
[[ -f ~/.zshrc.local ]] && source ~/.zshrc.local
```

## Требования

- Ubuntu 22.04+ / Debian 11+
- Zsh 5.8+
- Git
- Curl

## Обновление

```bash
cd ~/dotfiles
git pull
cp .zshrc ~/.zshrc
cp .p10k.zsh ~/.p10k.zsh
source ~/.zshrc
```

## Поддержка

При возникновении проблем создайте issue в репозитории.

---

**Автор:** Yan Shishenya
**Лицензия:** MIT
