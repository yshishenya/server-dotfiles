# 🚀 Крутые фичи Zsh

После обновления `.zshrc` у вас появятся супер-возможности!

## 📂 Умная навигация

```bash
# Переход БЕЗ cd - просто введите имя папки
projects           # → cd projects
/opt/projects      # → cd /opt/projects

# Быстрый возврат назад
..                 # → cd ..
...                # → cd ../..
....               # → cd ../../..

# Stack директорий (автоматический pushd)
cd dir1
cd dir2
cd dir3
d                  # показать stack
cd -1              # вернуться в dir2
cd -2              # вернуться в dir1

# Умный cd с памятью (zoxide)
z projects         # прыгнет в /opt/projects
z proj             # тоже сработает
cdi                # интерактивный выбор через fzf
```

## 📚 Улучшенная история

```bash
# 50,000 команд в истории с timestamps
# Без дубликатов, общая между сессиями

# Ctrl+R - поиск в истории через fzf
# Просто начните печатать и используйте стрелки

# Команды с пробелом НЕ сохраняются (для паролей)
 echo "secret"     # не попадет в историю

# История команд
history            # показать всю историю
history 20         # последние 20
history | grep git # поиск в истории
```

## ⌨️ Автодополнение

```bash
# Case-insensitive
git Ch<TAB>        # → git Checkout (работает несмотря на заглавную)

# Частичное совпадение
git co<TAB>        # → показать: commit, config, checkout...

# В середине слова
git com<TAB>mit    # → git commit

# С описаниями (цветные)
docker <TAB>       # покажет все команды с описаниями
```

## 🔧 Исправление опечаток

```bash
gti status         # → git status? [y/n/a/e]
cd /tpm            # → cd /tmp? [y/n/a/e]

# Нажмите:
# y - да, исправить
# n - нет, оставить как есть
# a - abort
# e - edit
```

## 🎯 Полезные функции

### mkcd - создать и перейти

```bash
mkcd new/deep/path  # создаст все папки и перейдет
```

### extract - распаковать что угодно

```bash
extract archive.tar.gz
extract file.zip
extract data.7z
# Автоматически определит тип и распакует
```

### backup - быстрый бэкап

```bash
backup important.conf
# Создаст: important.conf.backup-20250102-143052
```

### psgrep - найти процесс

```bash
psgrep node        # найти все процессы node
psgrep postgres
```

### killp - убить процесс по имени

```bash
killp node         # убьет все процессы node
killp python       # осторожно!
```

### vif - vim + fzf

```bash
vif                # интерактивный выбор файла
# Стрелками выбрать, Enter - открыть в vim
# С preview через bat!
```

### cdf - cd + fzf

```bash
cdf                # выбрать директорию через fzf
cdf /opt           # начать поиск с /opt
```

### gdiff - git + fzf

```bash
gdiff              # выбрать коммит и посмотреть diff
# С preview изменений!
# Enter - открыть полный diff в less
```

### dexec - docker exec + fzf

```bash
dexec              # выбрать контейнер и войти
# Автоматически попробует bash, потом sh
```

### dirsize - размер директорий

```bash
dirsize            # показать размер всех папок в текущей
dirsize /opt       # размер папок в /opt
# Отсортировано по убыванию
```

### replace - найти и заменить

```bash
replace "old_text" "new_text"
# Найдет во всех файлах (через rg) и заменит
```

## 🔍 FZF - супер-поиск

### Горячие клавиши

```bash
Ctrl+R             # Поиск в истории команд
Ctrl+T             # Выбрать файл
Alt+C              # Выбрать директорию
```

### Использование в командах

```bash
vim **<TAB>        # fzf выбор файла для vim
cd **<TAB>         # fzf выбор директории

kill -9 **<TAB>    # fzf выбор процесса для kill
ssh **<TAB>        # fzf выбор хоста из ~/.ssh/config
```

### FZF трюки

```bash
# Preview файлов
preview            # alias для fzf с bat preview

# Найти и открыть в vim
vf                 # alias: vim + fzf + preview

# Любая команда + fzf
docker ps | fzf
git branch | fzf
```

## 🐳 Docker команды

```bash
dps                # красивая таблица контейнеров
dcu                # docker compose up -d
dcd                # docker compose down
dcl                # docker compose logs -f
ld                 # lazydocker (TUI)
dexec              # войти в контейнер (fzf)
```

## 🎨 Цветной вывод

```bash
man git            # цветные man страницы
ls                 # eza с иконками и цветами
cat file.js        # bat с подсветкой синтаксиса
grep "text"        # ripgrep с цветами
git diff           # delta с side-by-side
```

## 🔒 Безопасность

```bash
rm file            # → спросит подтверждение
cp file1 file2     # → спросит если file2 существует
mv old new         # → спросит если new существует

# Чтобы без подтверждения:
\rm file           # оригинальный rm
/bin/rm file       # полный путь
```

## 🌐 Сетевые утилиты

```bash
myip               # узнать свой IP
ports              # показать все открытые порты
weather            # погода в терминале
```

## 📝 Git алиасы

```bash
lg                 # lazygit - TUI для git
gd                 # git diff (с delta)
gl                 # git log --graph
gdiff              # интерактивный выбор коммита (fzf)
```

## 🎓 Продвинутые трюки

### Глобальные алиасы

```bash
# Можно использовать в ЛЮБОМ месте команды
alias -g L='| less'
alias -g G='| grep'
alias -g H='| head'
alias -g T='| tail'

# Использование:
ps aux G docker L  # → ps aux | grep docker | less
dmesg T 20         # → dmesg | tail 20
```

### Расширенный glob

```bash
# Найти все JS файлы кроме node_modules
ls **/*.js~node_modules/**

# Все кроме скрытых
ls *~.*

# Только файлы (не директории)
ls *(.)

# Только директории
ls *(/)

# Файлы больше 10MB
ls *(Lm+10)

# Измененные за последний час
ls *(mh-1)
```

### Множественные команды

```bash
# Выполнить и показать в обоих файлах
echo "test" | tee file1.txt file2.txt

# С zsh MULTIOS:
echo "test" > file1.txt > file2.txt  # запишет в оба!
```

## 🚦 Подсказки для новичков

```bash
# Узнать что делает команда:
tldr docker        # краткие примеры
man docker         # полная документация

# Узнать где находится команда:
which node
type node

# Узнать что такое алиас:
alias ls           # покажет: ls='eza --icons...'

# Увидеть все алиасы:
alias

# Временно отключить алиас:
\ls                # оригинальный ls
```

## 💡 Полезные комбинации

```bash
# Найти большие файлы
fd . -S +100M

# Найти и удалить node_modules
fd node_modules -t d -X rm -rf

# Найти TODO в коде
rg TODO -t js -t py

# Мониторить изменения файла
watch -n 1 cat file.log

# Найти процессы по порту
lsof -i :3000

# Освободить RAM
sync && echo 3 | sudo tee /proc/sys/vm/drop_caches
```

## 🎬 Начало работы

После обновления dotfiles:

```bash
cd ~/dotfiles
git pull
cp .zshrc ~/.zshrc
exec zsh
```

Попробуйте:
1. `..` - подняться на уровень выше
2. `Ctrl+R` - поиск в истории
3. `vif` - открыть файл через fzf
4. `mkcd test/new/path` - создать и перейти
5. `gti status` - посмотреть исправление опечаток
6. `lg` - запустить lazygit

**Приятной работы!** 🚀
